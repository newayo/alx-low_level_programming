#include <stdio.h>

/**
 * main- the program print if the number is positive, zero or negative
 *
 * Description: the program prints using the main
 * function "Programming is positive, zero or negative
 * Return: 0
 */

int main(void)
{
	char ch;

	for (ch = 'z' ; ch >= 'a' ; ch--)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
