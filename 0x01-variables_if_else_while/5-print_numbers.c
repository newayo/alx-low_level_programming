#include <stdio.h>

/**
 * main - prints if the number is positive, zero or negative
 *
 * Description: he program prints using the main function
 * "Programming is positive, zero or negative
 * Return: 0
 */

int main(void)
{
	int n;

	for (n = 0 ; n < 10 ; n++)
	{
		printf("%n", n);
	}
	printf("\n");
	return (0);
}
